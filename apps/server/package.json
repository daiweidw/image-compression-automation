{
  "name": "@ica/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development tsx watch src/index.ts",
    "build": "tsup src/index.ts src/runtime.ts --format esm --platform node --target node22 --out-dir dist --external better-sqlite3 --external sharp --sourcemap",
    "start": "NODE_ENV=production node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/static": "^8.2.0",
    "@ica/contracts": "workspace:*",
    "better-sqlite3": "^12.2.0",
    "chokidar": "^5.0.0",
    "fastify": "^5.5.0",
    "sharp": "^0.34.3",
    "ulid": "^3.0.1",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.3.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
